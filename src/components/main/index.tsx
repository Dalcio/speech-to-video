import React from 'react';

import { Container } from './styled';

export const Main: React.FC = () => {
    return (
        <Container>
            <p>Main activity</p>
        </Container>
    );
};
